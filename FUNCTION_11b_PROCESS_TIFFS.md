# Function 11b: Upload JPG Files via Selenium (Part 2 of 2)

## Overview

**Function 11b** completes the JPG upload process started by Function 11a. It uses Selenium browser automation to upload processed JPG files to Alma representations via the web interface.

**Why Selenium?** The Alma API file upload endpoint has a limitation that prevents binary file uploads (UTF-8 parsing error). Function 11b works around this by automating the manual upload process through the Alma web interface.

## Prerequisites

### Required Software
1. **Firefox** web browser installed
2. **GeckoDriver** installed (required for Selenium to control Firefox)
   ```bash
   # macOS (via Homebrew)
   brew install geckodriver
   
   # Or download from: https://github.com/mozilla/geckodriver/releases
   ```
3. **Python selenium package** (should be installed via requirements.txt)

### Required Setup
1. **GeckoDriver installed** (Selenium Firefox driver)
   ```bash
   # macOS (via Homebrew)
   brew install geckodriver
   ```

2. **CSV file from Function 11a** containing representation IDs and file paths
   - Located in `~/Downloads/CABB_tiff_jpg_prep_YYYYMMDD_HHMMSS/`
   - Contains columns: `mms_id`, `rep_id`, `jpg_filename`, `tiff_filename`

### Workflow

**Simple Process:**

1. Click "11b - Upload JPG Files" in CABB
2. Enter the CSV file path in the "Set ID" field (should be auto-populated from Function 11a)
3. Click Proceed in the warning dialog
4. **Selenium launches a NEW Firefox window**
5. **You have 60 seconds to log into Alma via Grinnell SSO + DUO**
6. **If you need more time, automation pauses 30 additional seconds**
7. Automation begins automatically
8. Do not interact with Firefox during uploads

**Note**: Selenium **cannot** attach to your existing Firefox session—it must launch its own window. This is a Selenium/GeckoDriver limitation. The browser navigates to the SSO login page automatically (`https://grinnell.alma.exlibrisgroup.com/SAML`). The automation intelligently waits if you're still completing DUO authentication.

## How It Works

### Input
- **CSV File Path**: Enter the path to the CSV file generated by Function 11a in the "Set ID" field
- The CSV must have these columns:
  - `mms_id`: Bibliographic record MMS ID
  - `rep_id`: Representation ID (created by Function 11a)
  - `jpg_filename`: Full path to processed JPG file
  - `tiff_filename`: Full path to original TIFF file (for reference)

### Process Flow

For each record in the CSV file:

1. **Configure Search Bar** (First time only - settings persist)
   - Sets search type to "Digital titles"
   - Sets search field to "Representation PID"

2. **Search for Representation**
   - Enters the representation ID
   - Clicks search (magnifying glass icon)
   - Waits for results

3. **Open Digital Representations**
   - Finds and clicks "Digital Representations (X)" link
   - Waits for modal/window to open

4. **Select Specific Representation**
   - Locates the representation ID link
   - Clicks to open representation details

5. **Upload JPG File**
   - Finds file upload input field
   - Selects the JPG file from the prepared directory
   - Waits for file to be selected/processed

6. **Save**
   - Clicks "Save" button
   - Waits for save to complete
   - Moves to next record

## Usage

### Quick Start (Recommended)

**Selenium now automatically launches Firefox** - no manual browser setup needed!

### Step 1: Prepare CSV Path
1. Locate the CSV file path from Function 11a output
   - Should be auto-populated in the Set ID field
   - Example: `~/Downloads/CABB_tiff_jpg_prep_20260303_143052/tiff_jpg_representations_20260303_143052.csv`

### Step 2: Run Function 11b
1. In CABB, verify the CSV file path is in the **Set ID** field
2. Select **"11b: Upload JPG Files (Part 2 of 2)"** from the function dropdown
3. Click **"Run Function on Set"**
4. Review the warning dialog

### Step 3: Warning Dialog
Read the important information:
- **Close all Firefox windows** before proceeding
- Selenium will launch a **NEW** Firefox window
- You'll have 60 seconds to log into Alma
- Automation starts automatically after login

### Step 4: Click Proceed
1. Click the "Proceed" button in the warning dialog
2. Wait for Firefox to launch

### Step 5: Log into Alma
1. **Firefox window opens** at Grinnell SSO login page
2. **Complete Grinnell SSO authentication**
3. **Complete DUO two-factor authentication**
4. **Wait for Alma home page to fully load**
5. **IMPORTANT - Configure Search Bar:**
   - Click search type dropdown → Select "Digital titles"
   - Click search field dropdown → Select "Representation ID" or "Representation PID"
   - Leave search box EMPTY
   - This only needs to be done once - settings persist between searches

### Step 6: Wait for Automation
1. After 60 seconds, automation begins
2. **Do not touch the Firefox window during automation**
3. Watch the progress in CABB
4. Automation will:
   - Search for each representation
   - Open representation details
   - Upload JPG file
   - Save
   - Move to next record

### Step 7: Completion
1. Automation completes
2. **Firefox stays open** for your review
3. **Completion dialog** appears with summary
4. If all successful, option to **delete temp directory**
5. Close Firefox manually when done

## Automation Details

### Search Bar Configuration
**First Time Setup:**
The search bar in Alma needs to be configured before automation starts. This only needs to be done once, as Alma remembers the settings.

1. **Search Type dropdown** (left side): Select "Digital titles"
2. **Search Field dropdown** (middle): Select "Representation ID" or "Representation PID"  
3. **Search Box** (right side): Leave EMPTY

**Why Manual?** Selenium has difficulty reliably selecting dropdown options in Alma's dynamic interface. Manual setup is faster and more reliable.

### Popup Dismissal
The automation attempts to dismiss various popups automatically:
- "Stay signed in?" prompts
- "Manage Widgets" dialogs
- Cookie consent banners
- Generic modal overlays

If popups block automation, the script will retry and log warnings.

### File Upload
The JPG file is uploaded using a file input field. The automation:
1. Locates the file upload input (tries multiple selectors)
2. Sends the full file path to the input
3. Waits for file processing
4. Clicks Save

### Progress Tracking
- **Progress bar** shows current record number and percentage
- **Log messages** show each step for each record
- **Status updates** show current operation
- **Completion dialog** shows final summary

## Error Handling

### Common Errors

**"CSV file not found"**
- **Cause**: Invalid path in Set ID field
- **Solution**: Copy the correct path from Function 11a output

**"File not found"**
- **Cause**: JPG file doesn't exist at specified path
- **Solution**: Run Function 11a again to regenerate files

**"Could not start Firefox"**
- **Cause**: GeckoDriver not installed
- **Solution**: Run `brew install geckodriver` (macOS)

**"Digital Representations link not found"**
- **Cause**: Search returned no results, or wrong search settings
- **Solution**: Verify search is set to "Digital titles" / "Representation ID"

**"Could not find file upload input"**
- **Cause**: Representation details page didn't load correctly
- **Solution**: Check Firefox window, may need to restart

**"Save button not found"**
- **Cause**: Page structure changed or upload failed
- **Solution**: Check Firefox window, verify file was selected

### Failed Uploads
If any uploads fail:
1. A **failed CSV** is created in the same directory
2. The path is **auto-populated** in Set ID field
3. You can **re-run Function 11b** with just the failed records
4. Review Firefox window to diagnose issues

## Output

### Success
- All JPG files uploaded to Alma
- Representations now contain JPG files
- Temporary directory can be deleted

### Partial Success
- Some files uploaded, some failed
- Failed CSV created for retry
- Temporary directory preserved

### Completion Dialog
After automation completes, a dialog shows:
- Number of successful uploads
- Number of failed uploads
- Temporary directory location
- Option to delete temp directory (if all successful)

## Tips

### Optimization
- **Log Level**: Set to "ERROR" or "WARNING" for cleaner output
- **Close Firefox**: Close all Firefox windows before starting
- **Network**: Ensure stable network connection
- **Stay nearby**: Be ready to complete SSO login within 60 seconds

### Troubleshooting
- **Popups blocking?** Manually close them in Firefox
- **Search not finding reps?** Check search bar configuration
- **Upload not working?** Check Firefox window for errors
- **Slow uploads?** Large files take longer, be patient

### Safety
- **Kill Switch**: Use if automation gets stuck
- **Firefox stays open**: Review uploads manually after completion
- **Failed CSV**: Retry just the failed records

## Relationship to Function 14b

Function 11b follows the same pattern as Function 14b:
- Both use Selenium for file upload
- Both read CSV files from their "a" functions
- Both handle SSO/DUO authentication
- Both leave Firefox open for review
- Both create failed CSVs for retry

The main differences:
- 11b: Uploads JPG files to representations
- 14b: Uploads thumbnails to thumbnail fields

## Notes

- Selenium cannot use your existing Firefox session
- GeckoDriver must be installed separately
- Allow extra time for large JPG files
- Network issues may cause upload failures
- Firefox is left open for manual review
- Temporary directory can be deleted after successful upload
- Search bar settings persist between automation runs


## Purpose
This function processes TIFF files from their original locations, copies them to the `For-Import` directory, and creates JPG derivatives. It updates a CSV file with the filenames of both the JPG (file_name_1) and TIFF (file_name_2) for each object.

## How It Works

### Input Sources
1. **TIFF Location CSV**: `all_single_tiffs_with_local_paths.csv` - contains MMS IDs and local file paths for TIFF files
2. **MMS IDs**: Either a single MMS ID or a set of MMS IDs
3. **Optional Export CSV**: Path to alma_export CSV file (from Set ID field), or a new one will be created

### Process Flow
1. Reads `all_single_tiffs_with_local_paths.csv` to build a mapping of MMS ID → Local Path
2. For each MMS ID to process:
   - Locates the TIFF file using the Local Path
   - Copies the TIFF to `For-Import/` directory
   - Creates a JPG derivative (quality=95) in the same directory
   - Updates the CSV with:
     - `file_name_1`: JPG basename
     - `file_name_2`: TIFF basename

### Error Handling
- If a TIFF file is missing, logs an error and continues
- If file copy fails (e.g., network volume disconnected), logs error and continues
- If JPG creation fails, logs error and continues
- Creates `process_tiffs_failures.csv` with details of any failures

## Usage

### Single MMS ID Mode
1. Enter a single MMS ID in the MMS ID field
2. (Optional) Enter path to alma_export CSV in the Set ID field
3. Click "Process TIFFs & Create JPG Derivatives"

### Set Mode
1. Load a set using "Fetch Set Members"
2. (Optional) Enter path to alma_export CSV in the Set ID field
3. Click "Process TIFFs & Create JPG Derivatives"

## Output

### Files Created
- **For-Import/[basename].tif**: Copy of original TIFF
- **For-Import/[basename].jpg**: JPG derivative at quality=95
- **alma_export_[timestamp].csv**: CSV with file_name_1 and file_name_2 columns populated
- **process_tiffs_failures.csv**: (if errors occur) List of failed operations

### Progress Tracking
The function shows:
- Progress bar with percentage complete
- Current/total record count
- Status messages for each operation
- Summary of successful and failed operations

## Requirements
- `all_single_tiffs_with_local_paths.csv` must exist
- TIFF files must be accessible at the paths specified in the CSV
- Pillow library must be installed for JPG conversion

## Notes
- JPG quality is set to 95 for high-quality derivatives
- Original TIFF files are not modified, only copied
- Network volume disconnections may cause failures; check `process_tiffs_failures.csv`
- Progress is saved incrementally to the CSV file

## Recent Fixes (February 2026)

### 16-bit TIFF Conversion Fix
**Problem**: JPG derivatives were being created with correct dimensions but blank/white content when source TIFFs were 16-bit grayscale images.

**Root Cause**: 16-bit images (mode `I;16`) have pixel values ranging from 0-65,535. Direct conversion to 8-bit RGB was not properly scaling these values down to the 0-255 range required by JPG format, resulting in blank images.

**Solution**: Added proper bit-depth scaling for 16-bit images:
```python
if img.mode in ('I', 'I;16', 'I;16B', 'I;16L', 'I;16N'):
    # Properly scale 16-bit to 8-bit by dividing by 256
    img = img.point(lambda x: x / 256).convert('L').convert('RGB')
```

This divides pixel values by 256 (65,535 / 256 ≈ 255) before converting to 8-bit grayscale, then to RGB.

**Files Updated**:
- `app.py` (lines ~3032)
- `process_tiffs_for_import.py` (lines ~90)

### Status Message Improvements
**Changes**:
1. Changed "CSVs updated" to "CSV rows updated" for clarity (updating rows in one CSV, not multiple CSVs)
2. Added CSV filename to status message: `"100 CSV rows updated in alma_export_20260216_143052.csv"`
3. Fixed Set ID field being incorrectly interpreted as CSV filename when it contains an Alma set ID number
   - Now only treats Set ID field as CSV path if it contains `.csv`, `/`, or `\` characters
   - Otherwise generates timestamped filename automatically

**Example Status Messages**:
- Before: `"Function 12 complete: 100 TIFFs processed, 100 CSVs updated, 0 failed, 0 no path"`
- After: `"Function 12 complete: 100 TIFFs processed, 100 CSV rows updated in alma_export_20260216_143052.csv, 0 failed, 0 no path"`
